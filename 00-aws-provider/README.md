# 00-aws-provider

This example sets up a basic AWS provider configuration in Terraform.

## Files

- `main.tf`: Declares the AWS provider
- `variables.tf`: Defines the region as a variable

## Usage

```bash
terraform init
terraform plan
terraform apply
```
